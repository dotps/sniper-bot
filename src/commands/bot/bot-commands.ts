export enum BotCommands {
  Exit = "/exit",
  Start = "/start",
  AddToken = "/addtoken",
  RemoveToken = "/removetoken",
  Balance = "/balance",
  Follow = "/follow",
  Unfollow = "/unfollow",
  Replicate = "/replicate",
  Subscriptions = "/subscriptions",
  Send = "/send",
  Wallet = "/wallet",
}
