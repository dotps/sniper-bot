export enum Commands {
  EXIT = "/exit",
  START = "/start",
  ADD_TOKEN = "/addtoken",
  REMOVE_TOKEN = "/removetoken",
  BALANCE = "/balance",
  FOLLOW = "/follow",
  REPLICATE = "/replicate",
  SUBSCRIPTIONS = "/subscriptions",
}
